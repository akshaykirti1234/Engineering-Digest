package tech.csm;

import java.util.Arrays;
import java.util.List;
import java.util.function.Function;

public class Main {
	public static void main(String[] args) {
		Function<String, Integer> function = x -> x.length();

		Function<String, String> function2 = s -> s.substring(0, 3);

		Fucntion<List<Student>, List<Student>> stuNmStrtWthVip = student -> {
			return null;
		};

		System.out.println(function.apply("akshay"));
		System.out.println(function2.apply("akshay"));
		System.out.println(studentWithVipPrefix
				.apply(Arrays.asList(new Student(1, "akshay"), new Student(2, "vipul"), new Student(3, "Vipin"))));

	}

	private static class Student {
		private int id;
		private String name;

		public int getId() {
			return id;
		}

		public void setId(int id) {
			this.id = id;
		}

		public String getName() {
			return name;
		}

		public void setName(String name) {
			this.name = name;
		}

		public Student(int id, String name) {
			super();
			this.id = id;
			this.name = name;
		}

		@Override
		public String toString() {
			return "Student [id=" + id + ", name=" + name + "]";
		}

	}
}
